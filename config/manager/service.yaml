apiVersion: v1
kind: Service
metadata:
  name: cloudmanaged
spec:
  type: NodePort
  selector:
    control-plane: controller-manager
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
      nodePort: 30007