---
debugLogs: true
# kubeconfigPath: /path/to/kubeconfig

namespace: kuberlogic

# registry connection settings. omit for public access
# registry:
#   server: https://quay.io
#   username: quayusername
#   password: quaypassword

# connection endpoints configuration
endpoints:
  api: kuberlogic.com
  ui: kuberlogic.com
  monitoringConsole: mc.kuberlogic.com

auth:
  testUserPassword: secret
  adminPassword: adminPassword
