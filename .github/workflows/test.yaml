name: Run integration tests

on: [push]

jobs:
  example:
    name: Run integration tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Git credentials
        run: |
          git config --global url."https://$PRIVATE_GITHUB_TOKEN:x-oauth-basic@github.com/kuberlogic".insteadOf "https://github.com/kuberlogic"
        env:
          PRIVATE_GITHUB_TOKEN: ${{ secrets.PRIVATE_GITHUB_TOKEN }}

      - name: Get dependencies for the apiserver
        working-directory: ./modules/apiserver
        env:
          GOPRIVATE: github.com/kuberlogic/*
        run: |
          go mod download

      - name: Setup Minikube
        uses: manusa/actions-setup-minikube@v2.3.0
        with:
          minikube version: 'v1.16.0'
          kubernetes version: 'v1.19.2'
          github token: ${{ secrets.GITHUB_TOKEN }}

#      - name: kubectl config view
#        run: |
#          kubectl config view
#
#      - name: ls -la ${HOME}/.kube/config
#        run: |
#          ls -la ${HOME}/.kube/config
#
#      - name: cat ${HOME}/.kube/config
#        run: |
#          cat ${HOME}/.kube/config
#
#      - name: ls -la /home/runner/work/_temp/.minikube/profiles/minikube/
#        run: |
#          ls -la /home/runner/work/_temp/.minikube/profiles/minikube/
#
#      - name: cat /home/runner/work/_temp/.minikube/profiles/minikube/client.crt
#        run: |
#          cat /home/runner/work/_temp/.minikube/profiles/minikube/client.crt
#
#      - name: cat /home/runner/work/_temp/.minikube/profiles/minikube/client.key
#        run: |
#          cat /home/runner/work/_temp/.minikube/profiles/minikube/client.key

      - name: Create secret of operator
        run: |
          kubectl create secret docker-registry kuberlogic-registry --docker-server=$REGISTRY_SERVER --docker-username=$REGISTRY_USER --docker-password=$REGISTRY_PASSWORD
        env:
          REGISTRY_SERVER: ${{ secrets.REGISTRY_SERVER }}
          REGISTRY_USER: ${{ secrets.REGISTRY_USER }}
          REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Deploy operator
        run: |
          make deploy-dependencies

      - name: Waiting ready the pods
        run: |
          kubectl wait --for=condition=Ready pods --all --timeout=2m

      - name: Run the tests
        working-directory: ./modules/apiserver/tests
        run: |
          KUBERLOGIC_KUBECONFIGPATH=${HOME}/.kube/config make test

      - name: Archive logs
        uses: actions/upload-artifact@v2
        with:
          name: logs
          path: |
            ./modules/apiserver/tests/*.log
        if: ${{ always() }}
